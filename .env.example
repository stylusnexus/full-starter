# Base URL for your app under test
BASE_URL=http://localhost:3000

# Auth mode: set to '1' to bypass real authentication in tests
E2E_BYPASS_AUTH=1

# Real auth credentials (only needed when E2E_BYPASS_AUTH is not set)
# TEST_EMAIL=test@example.com
# TEST_PASSWORD=testpassword123
